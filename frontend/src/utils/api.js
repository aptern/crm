// Единый источник пространства имён бэкенд-эндпоинтов nacifrah (правило централизации, N1.5).
// Все вызовы идут через napi('module.method') → при переименовании app меняем ОДНУ строку,
// а не ~80 мест. Поведение идентично (та же строка пути).
export const NS = 'nacifrah'
export const napi = (path) => `${NS}.${path}`
